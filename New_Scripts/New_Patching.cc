#include <iostream>
#include <string>
using namespace std;

short* loc (short *a1, short *a2, int a1, int a2){
    short *ptr_1;
    short *enl1 = a1 + n1
    ptr_1 = a1;
    // Here we can use the next iteration in the next part
    for(int i=0; i<enl1; i++){
        if (*ptr_1 == *a1){
            for(int j=0; j<n2;j++){
                if (ptr_1 != a1){
                    break:
                }
            }
        }
     ptr_1++;
    }
    return 0;
}

// Here we invoke the next part of the main

int main() {
    // Here we declare the main function as we can see in the lines of the code
    float x = 3.45;
    float *ptr = &x;
    std::cout << *p << std::endl;
    float *q = new float;
}